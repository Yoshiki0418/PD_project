version: "3.8"

services:
  backend:
    build:
      context: .
      dockerfile: Dockerfile
    volumes:
      - ..:/workspace:cached
    ports:
      - "5001:5000"
    command: sleep infinity
    # command: ["uvicorn", "app.main:app", "--host", "0.0.0.0", "--reload"]
